{% extends 'base_admin.html.twig' %}

{% block title %}Cours{% endblock %}

{% block main %}
    <h1>Cours</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cour.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ cour.type }}</td>
            </tr>
            <tr>
                <th>Tarif</th>
                <td>{{ cour.tarif }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ cour.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_cours') }}">back to list</a>

    <a href="{{ path('admin_cours_edit', {'id': cour.id}) }}">edit</a>

    {{ include('admin_cours/_delete_form.html.twig') }}
{% endblock %}
