{% extends 'base_admin.html.twig' %}

{% block title %}Modification de Cours{% endblock %}

{% block main %}
<div class="form">
    <h1>Modification de Cours</h1>

    {{ include('admin_cours/_form.html.twig') }}

    <div class="backdelete">
        <div class="back">
            <a  class='btn' href="{{ path('admin_cours') }}">Retour à la liste</a>
        </div>
        <div class="delete">
            {{ include('admin_cours/_delete_form.html.twig') }}
        </div>
    </div>
</div>
    
{% endblock %}
