<form method="post" action="{{ path('admin_cours_delete', {'id': cour.id}) }}" onsubmit="return confirm('Êtes-vous sur de vouloir supprimer ce cours ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cour.id) }}">
    <button class="btn">Supprimer</button>
</form>
