<form method="post" action="{{ path('admin_samtosha_temoignages_delete', {'id': monTemoignage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ monTemoignage.id) }}">
    <button class="btn">Delete</button>
</form>
